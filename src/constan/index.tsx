// 设置的常量，当修改某个值时不需要没有页面都修改 只需要在当前文件修改即可
const KET_WORD = 'keyWord';
const PAGE_KEY = 'page';
const PAGE_SIZE_KEY = 'pageSize';
const PAGE_SIZE = 10;
export default KET_WORD;
export { PAGE_KEY, PAGE_SIZE_KEY, PAGE_SIZE };
